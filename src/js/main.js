/**
 * Created by fdimonte on 23/07/15.
 */

/********************
 *  ENTRY POINT
 ********************/
$(document).ready(function(){
    
    var ID = '#scrumdown',
        SD = new ScrumDown(ID),
        UI = new ScrumDownUI(SD);

});
